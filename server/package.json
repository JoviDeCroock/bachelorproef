{
  "name": "mock-server",
  "version": "0.0.1",
  "scripts": {
    "start": "json-server --watch data.json --port 8000"
  },
  "devDependencies": {},
  "dependencies": {
    "json-server": "^0.12.2"
  }
}
